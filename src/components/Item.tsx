import React from 'react';

import { TouchableOpacity, Text } from 'react-native';

function Item({ title, isCompleted }: any) {
    return (
        <TouchableOpacity>

        </TouchableOpacity>
    );
}